{
  "name": "contract-instructions-2025-offline",
  "private": true,
  "description": "دليل تعليمات تنفيذ العقود الحكومية رقم (1) لسنة 2025 — نسخة تعمل بلا إنترنت",
  "scripts": {
    "build": "node build.js",
    "check": "node build.js --check && node validate.js",
    "test": "node tests/ui.test.js",
    "verify": "npm run check && npm test"
  },
  "devDependencies": {
    "puppeteer-core": "^24.0.0"
  }
}
