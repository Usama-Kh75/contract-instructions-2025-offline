# يعمل مع كل رفع: يتأكد أن الملفات المنشورة مطابقة لمصادرها، وأن البيانات
# سليمة، وأن الواجهة تعمل بحجم الحاسوب والهاتف. علامة ✘ حمراء في GitHub
# تعني أن في الإصدار المرفوع خللاً يجب إصلاحه قبل أن يصل إلى الزملاء.
name: فحص الدليل

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      # v5: the v4 actions run on Node 20, which GitHub is retiring
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: المخرجات مطابقة للمصادر، والبيانات سليمة
        run: npm run check
      - name: اختبارات الواجهة (حاسوب وهاتف) والعمل بلا إنترنت والتحديث
        run: npm test
        env:
          CHROME_PATH: /usr/bin/google-chrome
